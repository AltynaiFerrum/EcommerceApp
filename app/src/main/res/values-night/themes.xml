<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcommerceApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.EcommerceApp" parent="Base.Theme.EcommerceApp" >
        <item name="mainBackgroundColor">@color/dark_background_color</item>
        <item name="TextColor">@color/white</item>
        <item name="like_icon">@drawable/heart</item>
        <item name="buy_icon">@drawable/buy</item>

        <item name="android:statusBarColor">@color/dark_background_color</item>
        <item name="VPbackgroundColor">@color/dark_background_color</item>
        <item name="search_background_color">@drawable/search_background_black</item>



    </style>

</resources>